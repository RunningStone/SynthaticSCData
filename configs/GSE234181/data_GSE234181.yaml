# ============================================================================
# Data Configuration: GSE234181 Dataset
# ============================================================================
# This file defines the data source, label mappings, and setting-specific
# configurations for training/testing data splits.

# Raw data file information
data_source:
  file_path: "/home/pan/Experiments/EXPs/2025_10_VCC_Exps/DATAs/GSE234181/GSE234181_with_label.h5ad"
  obs_time_column: "time_label"  # Column name in adata.obs for time labels
  
  # Time label order (cells with labels not in this list will be filtered out with warning)
  time_labels_order: ["T0", "T1", "T2", "T3"]
  
  # Time intervals between consecutive time points (in hours)
  # Adjust these based on your experimental design
  time_intervals:
    "T0-T1": 24  # 1 month = ~720 hours, using 24 as placeholder
    "T1-T2": 24  # 1 month = ~720 hours, using 24 as placeholder
    "T2-T3": 24  # Awakening phase, adjust as needed
  
  # Feature selection
  n_hvg: 100  # Number of highly variable genes to select

# Biology-based train/test split
# Split based on settings column values
biology_split:
  column_name: "settings"  # Column in adata.obs for split
  
  # Train samples
  train_values: [
    "T0_1",      # T0
    "AI1m1",     # T1
    "TAM1m1",    # T1
    "AI1m2",     # T2
    "TAM1m2",    # T2
    "TAMaw1",    # T3
    "TAMaw2",    # T3
    "TAMaw4",    # T3
    "AIaw2",     # T3
    "AIaw3",     # T3
    "AIaw4"      # T3
  ]
  
  # Test samples
  test_values: [
    "T0_2",      # T0
    "AI2m1",     # T1
    "TAM2m1",    # T1
    "AI2m2",     # T2
    "TAM2m2",    # T2
    "TAMaw3",    # T3
    "AIaw1"      # T3
  ]
  
  train_ratio: 0.8  # Only used if column_name is null (random split)

# Setting 1: Boundary time points only (first and last)
setting1:
  name: "Boundary Only"
  description: "Use only the first and last time points for interpolation (T0 and T3)"
  
  # Time points to include (must be in time_labels_order)
  time_points: ["T0", "T3"]
  
  # Number of cells per time point
  cells_per_timepoint: 2000
  
  # Validation: check if sufficient cells exist
  min_cells_required: 1000  # Error if any time point has fewer cells
  
  # Balance strategy
  balance_strategy: "per_timepoint"  # Each time point has cells_per_timepoint samples

# Setting 2: All time points
setting2:
  name: "All Time Points"
  description: "Use all available time points for full trajectory learning (T0, T1, T2, T3)"
  
  # Time points to include (must be in time_labels_order)
  time_points: ["T0", "T1", "T2", "T3"]
  
  # Total cells or cells per time point
  total_cells: 4000  # Total number of cells across all time points
  cells_per_timepoint: null  # If null, use total_cells; if set, use this instead
  
  # Validation
  min_cells_required: 500  # Error if any time point has fewer cells
  
  # Balance strategy: "total" or "per_timepoint"
  # - "total": Distribute total_cells evenly across time points
  # - "per_timepoint": Each time point has cells_per_timepoint samples
  balance_strategy: "total"

# Data validation settings
validation:
  check_all_timepoints_present: true  # Ensure both train and test have all time points
  warn_on_filtered_labels: true  # Warn if any cells are filtered due to unlisted labels
  check_sufficient_cells: true  # Check min_cells_required for each time point
