# 实验4-7实现可行性总体评估

## 评估摘要

本文档评估新设计的实验4-7在现有SynthaticSCData项目框架下的实现可行性。总体结论：**四个实验均可在现有架构上以最小修改实现，无需重构核心模块**。

---

## 现有系统能力盘点

### 已实现的核心功能

1. **配置驱动的数据采样系统**
   - 支持灵活的时间点选择：`time_points: ["0d", "8h", "7d"]`
   - 支持两种采样策略：`per_timepoint`（固定每点样本数）和`total`（总量均分）
   - 自动计算采样参数：`step0_calculate_data_split_params.py`
   - 已有6个预定义setting（Part1和Part2各3个）

2. **完整的模型训练流程**
   - 统一训练接口：`Trainer/utils.py::train_model()`
   - 支持5种模型：SB, SB_MLPlus, OT, VAE, Batch OT
   - 自动保存检查点、训练历史、评估结果
   - 早停、学习率调度、梯度裁剪等机制完备

3. **标准化评估体系**
   - 10个评估指标：Test Loss, Fréchet Distance, MAE, PCC, Wasserstein, MMD, R², JS散度, 相关矩阵差异, 相关结构相关性
   - 可配置的起点和终点：`Evaluator(start_timepoint='0d', end_timepoint='7d')`
   - 支持跨setting的模型对比可视化

4. **轨迹生成能力**
   - SB模型通过SDE求解生成连续轨迹：`generate_trajectory(x0, time_grid)`
   - 支持确定性（无噪声）和随机性（带噪声）生成
   - 时间步长可配置（默认0.01）

### 系统架构优势

- **模块化设计**：Data, Model, Trainer, Analyser四大模块职责清晰
- **配置文件驱动**：新增实验只需添加YAML配置，无需修改代码
- **可扩展性强**：评估器支持自定义指标，可视化系统支持任意数量setting对比

---

## 四个实验的实现路径分类

### 类型A：纯配置扩展（实验4）
**特征**：仅需新增YAML配置文件，复用全部现有代码  
**工作量**：极低（1-2小时）  
**风险**：无

### 类型B：轻量代码扩展（实验5, 6）
**特征**：需要新增数据预处理或训练逻辑，但不涉及核心模块修改  
**工作量**：低（半天到1天）  
**风险**：低（新增代码与现有系统解耦）

### 类型C：评估指标扩展（实验7）
**特征**：需要实现新的评估指标（熵估计），但评估框架已完备  
**工作量**：中等（1-2天）  
**风险**：中等（熵估计算法需验证正确性）

---

## 关键技术依赖检查

### 实验4：时间点消融
- ✅ **数据采样**：现有`RealDataLoader`支持任意时间点子集
- ✅ **模型训练**：复用SB_MLPlus训练流程
- ✅ **评估指标**：10个标准指标已实现
- ⚠️ **需补充**：边际贡献分析脚本（后处理，非核心流程）

### 实验5：时间打乱
- ✅ **时间编码**：SB模型已支持任意时间标签输入
- ✅ **配对生成**：可在Dataset层实现随机配对逻辑
- ⚠️ **需新增**：打乱数据集构造器（约200行代码）
- ⚠️ **需补充**：轨迹连续性指标（约50行代码）

### 实验6：合成插值
- ✅ **边界数据**：Setting1已提供
- ✅ **线性插值**：NumPy/PyTorch原生支持
- ⚠️ **需新增**：插值数据生成脚本（约150行代码）
- ⚠️ **需补充**：中间时间点专项误差分析（约100行代码）

### 实验7：熵演化
- ✅ **轨迹生成**：SB模型的`generate_trajectory()`已实现
- ✅ **多时间点采样**：测试集包含所有时间点
- ⚠️ **需新增**：KNN熵估计器（约100行代码，可用`scikit-learn`）
- ⚠️ **需新增**：高斯熵估计器（约80行代码，基于协方差矩阵）
- ⚠️ **需补充**：熵曲线分析和可视化（约150行代码）

---

## 代码复用率估算

| 实验 | 可复用代码 | 新增代码量 | 复用率 |
|------|-----------|-----------|--------|
| 实验4 | 100% | 0行（仅配置） | 100% |
| 实验5 | 90% | ~250行 | 90% |
| 实验6 | 85% | ~250行 | 85% |
| 实验7 | 80% | ~330行 | 80% |

**总体复用率**：约89%，说明现有架构高度适配新实验需求。

---

## 实现优先级建议

### 第一阶段（立即可做）
**实验4：时间点消融**  
- 原因：零代码修改，纯配置扩展
- 产出：快速验证哪些时间点最关键
- 时间：1-2小时

### 第二阶段（短期实现）
**实验7：熵演化**  
- 原因：科学价值最高，直接验证核心假设
- 依赖：需要实验4的训练好的模型
- 时间：1-2天

### 第三阶段（中期实现）
**实验5：时间打乱**  
- 原因：理论意义重大，区分时间因果与空间覆盖
- 依赖：需要新训练打乱数据的模型
- 时间：1天

**实验6：合成插值**  
- 原因：排除竞争解释，增强论证完整性
- 依赖：需要新训练插值数据的模型
- 时间：1天

---

## 潜在技术挑战与解决方案

### 挑战1：高维熵估计的准确性
**问题**：1000维基因表达数据，KNN和高斯方法都可能有偏  
**解决方案**：
- 使用两种方法交叉验证
- 预先降维到50-100维（PCA或PHATE）
- 参考文献中的超参数设置（k=5, Ledoit-Wolf收缩）

### 挑战2：打乱数据的时间间隔分布匹配
**问题**：需要保持$\Delta t$分布一致，避免引入混淆因素  
**解决方案**：
- 统计真实数据的时间间隔直方图
- 按频率采样$\Delta t$，然后随机选择起点和终点
- 验证打乱后的$\Delta t$分布与原始分布的KS检验p>0.05

### 挑战3：插值方法的计算效率
**问题**：线性插值简单但可能不够精确  
**解决方案**：
- 先实现线性插值（最简单，作为下界）
- 如果线性插值失败，说明问题已解决（真实数据不可插值）
- 如果线性插值成功，再考虑更复杂方法（但这反而削弱论点）

---

## 数据和计算资源需求

### 存储需求
- 每个新setting的模型检查点：~50MB（SB_MLPlus）
- 实验4：4个消融变体 × 50MB = 200MB
- 实验5：1个打乱setting × 50MB = 50MB
- 实验6：1个插值setting × 5个模型 × 50MB = 250MB
- 实验7：无额外模型，仅评估结果 < 10MB
- **总计**：约500MB，完全可接受

### 计算资源需求
- 单个SB_MLPlus训练时间：约2-4小时（GPU）
- 实验4：4个消融 × 3小时 = 12小时
- 实验5：1个打乱 × 3小时 = 3小时
- 实验6：1个插值 × 5个模型 × 3小时 = 15小时
- 实验7：轨迹生成和熵估计 < 1小时（CPU即可）
- **总计**：约31小时GPU时间，可在1-2天内完成（并行训练）

---

## 风险评估与缓解措施

### 低风险项
- ✅ 实验4：纯配置扩展，无技术风险
- ✅ 实验6：线性插值算法简单，实现风险低

### 中风险项
- ⚠️ 实验5：打乱逻辑需仔细验证时间间隔分布匹配
  - **缓解**：编写单元测试，绘制$\Delta t$分布对比图
- ⚠️ 实验7：熵估计在高维下可能不稳定
  - **缓解**：使用两种方法交叉验证，预先降维

### 高风险项
- ❌ 无高风险项

---

## 实施建议

### 开发策略
1. **先实验后优化**：快速实现基础版本，验证科学假设后再优化代码
2. **模块化开发**：每个实验独立脚本，避免相互干扰
3. **增量测试**：每完成一个实验立即运行，避免积累bug

### 代码组织
建议在项目根目录新增`Experiments/`文件夹：
```
SynthaticSCData/
├── Experiments/
│   ├── exp4_ablation/
│   │   ├── configs/  # 消融setting配置
│   │   └── analyze_marginal_contribution.py
│   ├── exp5_shuffle/
│   │   ├── shuffled_dataset.py
│   │   └── analyze_trajectory_continuity.py
│   ├── exp6_interpolation/
│   │   ├── generate_interpolated_data.py
│   │   └── analyze_interpolation_residuals.py
│   └── exp7_entropy/
│       ├── entropy_estimators.py
│       └── analyze_entropy_evolution.py
```

### 文档要求
每个实验脚本需包含：
- 详细的docstring说明实验目的和参数
- 输入输出格式的明确定义
- 运行示例和预期结果描述

---

## 结论

**总体可行性**：⭐⭐⭐⭐⭐（5/5星）

四个实验均可在现有架构上高效实现，无需重构核心模块。实验4可立即开始（零代码修改），实验5-7需要1-2天开发时间。总计约3-4天可完成全部实验的代码实现和初步验证。

**关键成功因素**：
1. 现有配置系统的灵活性（支持任意时间点组合）
2. 评估框架的可扩展性（易于添加新指标）
3. 模型训练流程的标准化（减少重复工作）

**建议行动**：
1. 立即启动实验4（时间点消融），验证系统兼容性
2. 并行开发实验7的熵估计模块（科学价值最高）
3. 根据实验4和7的结果，决定实验5和6的优先级
