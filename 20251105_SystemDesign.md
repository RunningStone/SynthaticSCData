# 真实数据细胞状态转换学习系统设计

## 一、系统概述与设计理念

本系统研究单细胞时间序列数据建模中的核心问题：在学习细胞状态转换动力学时，完整的时间轨迹信息相比仅有起止点信息能否带来显著的性能提升。这个问题直接影响实验设计和数据收集策略的制定。

系统采用配置驱动的模块化架构，支持灵活定义不同的实验设置。每个实验设置通过YAML配置文件指定数据源、时间点选择、模型配置和评估策略。当前实现包含六个预定义设置，覆盖从简单边界插值到复杂双向轨迹学习的多种场景。Setting 1和Setting 5使用两个边界时间点，模拟低时间分辨率实验。Setting 2和Setting 4使用所有可用时间点，模拟高时间分辨率实验。Setting 3和Setting 6使用三个关键时间点，探索中等分辨率下的性能。

数据集来自EMT实验，包含前向EMT过程和刺激移除后的逆转过程。前向过程包括0天基线、8小时早期响应、1天、3天和7天峰值状态。逆转过程包括7天峰值后移除刺激，观察8小时、1天和3天的恢复状态。这种双向动态过程为研究可逆性细胞状态转换提供了理想的测试平台。

模型选择基于Schrödinger Bridge理论框架，该框架通过学习时间依赖的漂移场描述细胞状态演化。数学上等价于最小化路径测度与参考扩散过程之间的KL散度在时间区间上的积分。这种表述天然地将时间信息融入优化目标，使模型能够捕捉状态转换的时序依赖性。系统同时实现了Optimal Transport、Conditional VAE和Batch OT等对比模型，用于验证Schrödinger Bridge方法的优势。

## 二、配置系统与数据处理

配置系统采用分层设计，将数据、模型和实验设置分离到独立的YAML文件中。数据配置文件定义数据源路径、时间标签列名、时间点顺序和生物学划分策略。模型配置文件定义所有可用模型的网络架构和训练参数。实验配置文件引用这些基础配置，指定要使用的数据设置和要训练的模型列表。这种分层设计使得修改某个方面的配置不会影响其他部分，提高了系统的可维护性。

ConfigLoader类负责加载和合并配置文件。加载实验配置时，系统首先读取实验YAML文件，提取其中引用的数据、模型和分析器配置文件名。然后加载这些基础配置文件，根据实验配置中指定的数据设置名称提取相应的时间点和采样参数。对于每个启用的模型，系统从模型配置中提取基础参数，并应用实验配置中的覆盖参数。最终生成一个完整的配置字典，包含运行实验所需的所有信息。

数据处理流程由RealDataLoader类实现。系统接收AnnData格式的h5ad文件作为输入，首先进行高变基因筛选将基因数量降维到指定值。然后根据时间标签列筛选出配置中指定的时间点，过滤掉不在时间点列表中的细胞。生物学划分确保训练集和测试集来自不同的实验批次，系统验证每个划分是否包含所有时间点，避免数据泄露。

数据采样策略支持两种模式。per_timepoint模式为每个时间点采样固定数量的细胞，适用于边界设置。total模式指定总细胞数并均匀分配到各时间点，适用于多时间点设置。系统还支持实验级别的采样参数覆盖，允许在不修改数据配置的情况下调整采样数量。采样后的数据被封装为PyTorch Dataset和DataLoader，批次大小通常设为256，训练集启用随机打乱。

## 三、模型架构设计

系统实现了五种生成模型用于细胞状态转换学习。Schrödinger Bridge模型是核心方法，通过学习时间依赖的漂移场描述细胞状态演化。其他四种模型作为对比基线，验证SB方法的优势。

基础Schrödinger Bridge模型将细胞状态转换建模为随机微分方程，漂移场由双势函数网络参数化。每个势函数使用四层MLP实现，隐藏层维度512，包含ReLU激活和Dropout正则化。时间信息通过正弦位置编码嵌入到64维向量，与状态向量拼接后输入网络。训练目标是最小化预测速度与经验速度之间的均方误差，使模型学习局部时间导数捕捉瞬时动力学。

MLPlus增强模型针对多时间点场景进行优化。引入多尺度时间编码使用10个可学习频率的正弦基函数，能够同时捕捉快速和缓慢的动力学变化。网络主干采用4个残差块堆叠，每个残差块包含两层全连接网络和跳跃连接，缓解梯度消失问题。每个残差块后添加Layer Normalization稳定训练过程，处理不同时间点的分布差异。总参数量约470万，相比基础版本的270万有显著增加。

Optimal Transport模型学习从初始分布到目标分布的确定性映射，通过最小化Wasserstein-2距离实现。网络使用四层MLP参数化映射函数，包含残差连接提高表达能力。训练时最小化预测终点与真实终点的欧氏距离，不涉及中间时间点的动力学建模。

Conditional VAE模型采用变分自编码器框架，编码器将起始状态和时间条件映射到128维潜在空间，解码器重构目标状态。损失函数包含重构项和KL散度项，平衡重构质量和潜在空间的正则化。VAE的概率性质能够捕捉状态转换的不确定性，但缺乏显式的时间依赖建模。

Batch OT模型针对多时间点场景设计，学习多个连续时间段的OT映射。训练时为每对相邻时间点学习独立的映射函数，推理时顺序应用这些映射并在离散状态间进行线性插值。这种方法将长时间跨度的映射分解为多个短时间段的映射，降低学习难度但增加了模型复杂度。

## 四、训练框架与优化策略

训练系统采用统一的优化框架，支持多种模型类型。SBTrainer类处理Schrödinger Bridge模型的训练，UnifiedTrainer类处理OT和VAE模型，BatchOTTrainer类处理批量OT模型。所有训练器共享相同的优化配置和训练流程，确保不同模型之间的公平比较。

优化器使用AdamW，学习率设为5e-4，权重衰减系数1e-5提供L2正则化。批次大小固定为256，在计算效率和梯度估计质量之间取得平衡。学习率调度采用ReduceLROnPlateau策略，当验证损失在10个epoch内未改善时，学习率衰减至原来的一半，最低降至1e-6。梯度裁剪设置最大范数为5.0，防止梯度爆炸导致的训练不稳定。

早停机制监控验证集损失，若连续30个epoch无改善则终止训练，避免过拟合并节省计算资源。系统自动保存验证损失最低的模型检查点，确保最终使用的是泛化性能最佳的参数配置。训练历史记录每个epoch的训练损失、验证损失和学习率，保存为JSON文件供事后分析。

训练数据的组织方式取决于实验设置。对于边界设置，训练数据由起止点配对构成。对于多时间点设置，训练数据包含所有连续时间点对，提供更密集的时间监督信号。Schrödinger Bridge模型从这些时间点对中学习局部速度场，而OT和VAE模型学习直接的源到目标映射。Batch OT模型为每对相邻时间点训练独立的映射函数。

训练流程由utils模块中的train_model函数统一协调。该函数根据模型类型选择合适的训练器，创建数据加载器，执行训练循环，并在训练结束后调用评估器计算所有指标。整个流程完全自动化，用户只需提供配置文件即可完成从数据加载到结果保存的全部步骤。

## 五、评估体系与可视化

评估系统由Evaluator类实现，计算十个标准指标量化模型性能。评估器支持配置化的起点和终点时间点，适应不同实验设置的需求。对于包含逆转过程的设置，可以指定评估从0天到3天逆转终点的转换，而不是默认的第一个到最后一个时间点。

测试集损失直接反映模型在未见数据上的拟合质量。对于Schrödinger Bridge模型，测试损失为速度场预测误差。由于速度是表达值变化除以时间间隔，损失值的量级会被时间间隔的倒数放大，解释时需要考虑时间尺度的影响。

Fréchet距离衡量生成分布与真实分布之间的差异，同时考虑均值和协方差矩阵的差异。该指标能够捕捉分布形状的变化，较小的FD值表示生成的细胞状态分布与真实分布更接近，意味着模型不仅能预测平均趋势，还能保持群体异质性。

平均绝对误差计算预测终点状态与真实终点状态之间的逐元素平均绝对误差，直观反映预测的点估计精度。Pearson相关系数衡量预测值与真实值之间的线性相关性，关注的是预测模式而非绝对值。

系统还计算Wasserstein距离、最大均值差异、R平方均值、JS散度、相关矩阵Frobenius差异和相关结构相关性等高级指标。这些指标从不同角度评估模型的预测质量，包括分布匹配、方差解释、信息散度和相关结构保持等方面。

可视化系统由MultiSettingVisualizer类实现，支持跨设置的模型对比。系统从每个设置的测试集中采样数据，加载所有训练好的模型，使用源时间点的数据生成目标时间点的预测。然后计算两种降维嵌入：PHATE嵌入保留全局流形结构，LMNN+PCA嵌入强调时间点分离。最后生成指标对比图和降维可视化图，直观展示不同模型和设置的性能差异。

## 六、实验设置与执行流程

系统当前包含六个预定义的实验设置，每个设置对应一个独立的YAML配置文件。Setting 1使用0天和7天两个边界时间点，模拟低时间分辨率的前向EMT实验。Setting 2使用所有五个前向EMT时间点，模拟高时间分辨率实验。Setting 3使用0天、8小时和7天三个关键时间点，探索中等分辨率下的性能。Setting 4使用所有八个时间点包括前向和逆转过程，研究双向动态建模。Setting 5使用0天和3天逆转终点两个边界点，测试跨越峰值的长距离插值。Setting 6使用0天、7天峰值和3天逆转终点三个关键点，研究峰值信息对逆转预测的帮助。

实验执行流程完全自动化。用户通过命令行指定实验配置文件，系统加载并合并所有引用的配置，验证数据可用性和参数合理性。然后根据配置创建数据加载器，为每个启用的模型创建相应的训练器实例。训练过程包含早停和学习率调度，自动保存最佳检查点。训练结束后评估器在测试集上计算所有指标，结果保存为JSON文件包含完整的训练历史和评估指标。

可视化流程支持同时对比多个实验设置。用户指定多个实验配置文件路径，系统自动聚合所有训练好的模型。从每个设置的测试集中采样数据，加载模型生成预测，计算降维嵌入，生成对比图表。输出包括指标对比条形图、PHATE可视化图、LMNN+PCA可视化图和CSV格式的详细数值结果。

核心研究问题的验证依赖于跨设置的性能对比。若多时间点设置在所有指标上优于边界设置，则说明完整时间轨迹信息确实提升了模型的泛化能力。若中等分辨率设置的性能接近高分辨率设置，则说明关键时间点的选择比时间点数量更重要。若包含峰值信息的设置能更好地预测逆转过程，则说明中间状态对理解可逆转换至关重要。这些发现将为实验设计和数据收集策略提供重要指导。

## 七、技术实现与系统架构

系统采用模块化设计，代码组织清晰且易于扩展。Data模块包含RealDataLoader类处理h5ad文件读取和HVG筛选，ConfigLoader类实现配置文件的加载和合并，dataset_builder模块构建PyTorch数据集和加载器。Model模块实现五种生成模型，sb_model和sb_model_mlplus是核心Schrödinger Bridge实现，ot_model、c_vae_model、vae_model和batch_ot_model提供对比基线。Trainer模块包含SBTrainer、UnifiedTrainer和BatchOTTrainer三个训练器类，Evaluator类计算所有评估指标，utils模块提供统一的训练协调函数。Analyser模块实现跨设置可视化，MultiSettingVisualizer类协调整个可视化流程。

关键参数配置通过YAML文件管理。数据层面，高变基因数量通常设为1000，时间点序列包含前向EMT和逆转过程的八个时间点。不同设置的采样策略通过配置文件灵活指定，支持per_timepoint和total两种模式。模型层面，基础SB模型使用四层隐藏层每层512神经元，时间编码维度64，扩散系数0.1，Dropout概率0.1。MLPlus模型增加到4个残差块，多尺度时间编码使用10个频率，总参数量约470万。训练层面，AdamW优化器学习率5e-4，权重衰减1e-5，批次大小256，最大训练轮数100到200，早停耐心值30，梯度裁剪范数5.0。

推理过程中，Schrödinger Bridge模型通过数值求解随机微分方程生成轨迹。使用Euler-Maruyama方法离散化SDE，时间步长通常设为0.01，迭代更新状态。对于确定性预测，可忽略噪声项仅保留漂移项。OT和VAE模型直接应用学习到的映射函数，Batch OT模型顺序应用多个映射并在离散状态间插值。生成的轨迹可用于可视化细胞状态演化路径，或提取特定时间点的状态进行下游分析。

系统支持灵活的扩展和定制。用户可以通过修改配置文件调整模型架构、训练参数和评估策略，无需修改代码。可以添加新的数据配置支持不同的数据集，添加新的模型配置集成其他生成模型。评估器支持配置化的起点和终点时间点，适应包含逆转过程等复杂场景。可视化系统支持任意数量的实验设置对比，自动调整图表布局。

## 八、实验目标

从方法学角度，系统展示了Schrödinger Bridge框架在单细胞动力学建模中的优势。相比于仅学习起止点映射的方法，SB模型通过时间依赖的漂移场捕捉演化过程的瞬时动力学，这对于理解细胞命运决定和状态转换机制至关重要。MLPlus增强模型针对多时间点数据的优化设计，说明了网络架构对利用时间信息的重要性。

系统的配置驱动架构使其易于扩展到其他应用场景。可以替换为不同的单细胞数据集研究分化、应激响应等过程，可以集成其他生成模型如Flow Matching或Score-based Models进行对比。评估指标可以根据具体问题调整，添加生物学相关的指标如基因调控网络推断或轨迹拓扑结构分析。这种灵活性使系统成为研究细胞状态转换动力学的通用平台。

对于包含逆转过程的数据集，系统能够研究可逆性细胞状态转换的建模问题。通过对比前向和逆转过程的预测质量，可以评估模型是否真正学到了可逆的动力学规律，还是仅仅记忆了训练数据的模式。这对于理解细胞可塑性和状态转换的可逆性具有重要意义。

