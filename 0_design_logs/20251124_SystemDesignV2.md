# 单细胞时间序列状态转换学习系统 - 架构设计文档

## 系统概述

本系统研究单细胞时间序列数据建模中的核心科学问题：在学习细胞状态转换动力学时，完整的时间轨迹信息相比仅有起止点信息能否带来显著的性能提升。系统采用配置驱动的模块化架构，通过统一的抽象接口支持多种生成模型的训练、评估和可视化对比。当前实现基于EMT数据集，包含前向EMT过程和刺激移除后的逆转过程，为研究可逆性细胞状态转换提供了理想的测试平台。

系统设计遵循原子化和模块化原则，将功能分解为独立的、可复用的组件。每个模块通过清晰定义的抽象基类提供统一接口，具体实现通过继承和组合实现功能扩展。这种设计使得添加新模型、新数据处理策略或新评估指标变得简单直接，同时保持代码的可维护性和可测试性。

## 模块架构设计

### Model模块：生成模型抽象层

Model模块定义了所有生成模型的统一接口和基础架构。系统通过三层抽象实现模型的组织：最顶层是BaseTrajectoryModel抽象基类，定义了所有轨迹生成模型必须实现的核心接口，包括前向传播、损失计算和轨迹生成三个基本操作。中间层根据模型的时间处理方式分为两个子类：ContinuousTimeModel用于处理连续时间动力学的模型，TimeConditionedModel用于处理离散时间条件的模型。底层是具体的模型实现，每个模型类继承相应的中间层基类并实现特定的网络架构和训练逻辑。

Schrödinger Bridge模型将细胞状态转换建模为随机微分方程，通过学习时间依赖的漂移场描述状态演化。基础SB模型使用双势函数网络参数化漂移场，每个势函数由多层感知机实现，时间信息通过正弦位置编码嵌入。MLPlus增强版本针对多时间点场景优化，引入多尺度时间编码和残差网络结构，能够同时捕捉快速和缓慢的动力学变化。Optimal Transport模型学习从初始分布到目标分布的确定性映射，通过最小化Wasserstein距离实现分布匹配。Conditional VAE模型采用变分自编码器框架，编码器将起始状态和时间条件映射到潜在空间，解码器重构目标状态。Batch OT模型针对多时间点场景设计，学习多个连续时间段的独立OT映射，推理时顺序应用这些映射。

所有模型通过统一的generate_trajectory接口生成轨迹预测，该接口接收源状态、时间网格和生成方法作为输入，返回完整的时间序列轨迹。这种统一接口使得不同模型可以在相同的评估框架下进行公平比较，同时简化了上层调用逻辑。模型的前向传播和损失计算也遵循统一的签名，训练器可以用相同的方式处理不同类型的模型。

### Trainer模块：训练流程抽象层

Trainer模块提供统一的训练框架，将训练流程中的通用逻辑抽象到基类中，具体模型的特殊需求通过子类定制实现。BaseTrainer抽象基类定义了完整的训练生命周期，包括优化器和学习率调度器的初始化、训练循环的执行、模型检查点的保存和加载、早停机制的实现以及训练历史的记录。

训练循环采用模板方法模式，基类实现循环的骨架逻辑，子类通过重写特定方法定制批次数据的处理和损失的计算。每个训练周期包含训练阶段和验证阶段，训练阶段启用梯度计算和参数更新，验证阶段禁用梯度计算仅评估模型性能。基类自动处理学习率调度、梯度裁剪、早停判断和最佳模型保存等通用操作，子类只需关注模型特定的数据处理和损失计算逻辑。

SBTrainer处理Schrödinger Bridge模型的训练，从批次数据中提取源状态、目标状态和时间信息，计算速度场预测误差作为损失。UnifiedTrainer处理Optimal Transport和Conditional VAE模型的训练，根据模型类型选择相应的损失计算方式。BatchOTTrainer处理批量OT模型的训练，为每对相邻时间点训练独立的映射函数，采用组合模式管理多个子模型的训练。

优化配置在所有训练器中保持一致，使用AdamW优化器配合ReduceLROnPlateau学习率调度策略。梯度裁剪防止训练不稳定，早停机制避免过拟合。训练历史记录每个周期的损失和学习率，保存为JSON文件供事后分析。检查点保存包含模型状态、优化器状态和训练配置，支持从中断点恢复训练。

### Data模块：数据处理抽象层

Data模块负责数据的加载、预处理和组织，通过抽象基类定义统一的数据处理流程。BaseDataLoader抽象基类实现了数据加载的完整生命周期，包括h5ad文件的读取、高变基因的筛选、时间点的过滤、生物学划分的验证、训练测试集的划分以及PyTorch数据加载器的创建。

数据加载流程采用模板方法模式，基类实现通用的加载和预处理步骤，子类通过重写特定方法实现不同的数据处理策略。RealDataLoader处理标准的h5ad文件，按照配置中指定的时间点和采样策略准备数据。ShuffledDataLoader在标准流程基础上打乱时间标签，用于研究时间信息对模型学习的影响。InterpolatedDataLoader在相邻时间点之间生成线性插值的中间状态，用于研究数据增强对模型泛化能力的影响。

高变基因筛选使用Scanpy库的标准流程，根据基因的离散度选择最具信息量的基因子集，将特征维度降低到可管理的范围。时间点过滤确保只保留配置中指定的时间点，移除不相关的观测。生物学划分验证确保训练集和测试集来自不同的实验批次，避免数据泄露导致的性能高估。

数据采样支持两种模式：per_timepoint模式为每个时间点采样固定数量的细胞，适用于边界设置；total模式指定总细胞数并均匀分配到各时间点，适用于多时间点设置。采样后的数据被封装为PyTorch的Dataset和DataLoader，批次大小通常设为256，训练集启用随机打乱以提高训练稳定性。

配置系统采用分层设计，将数据、模型和实验设置分离到独立的YAML文件中。ConfigLoader类负责加载和合并这些配置文件，根据实验配置中的引用关系自动加载基础配置，提取相应的参数并应用覆盖规则。这种分层设计使得修改某个方面的配置不会影响其他部分，提高了系统的灵活性和可维护性。

### Analyser模块：可视化分析层

Analyser模块实现了模块化的可视化和分析功能，通过六个独立组件提供完整的分析流程。模块设计遵循单一职责原则，每个组件专注于特定的功能，组件之间通过清晰的接口进行交互。

BaseVisualizer提供所有可视化组件的通用基础设施，包括输出目录的管理、计算设备的选择、随机种子的设置、图表和数据文件的保存以及格式化的日志输出。该基类定义了可视化组件的标准生命周期，子类继承这些通用功能并专注于特定的可视化逻辑。

DataManager负责数据的加载和管理操作，提供统一的接口加载h5ad文件、生成数据的pickle文件和评估指标的JSON文件。该组件封装了数据采样、时间点筛选和源目标样本提取等常用操作，简化了上层调用逻辑。数据加载过程包含错误处理和验证，确保加载的数据符合预期格式。

ModelManager处理模型的加载和推理操作，支持所有模型类型的统一加载接口。该组件根据模型类型和配置参数实例化相应的模型类，加载训练好的检查点，将模型移动到指定设备并设置为评估模式。轨迹生成接口封装了不同模型的生成逻辑，提供统一的调用方式。批量生成功能支持同时处理多个模型，提高计算效率。

EmbeddingComputer实现降维嵌入的计算，支持PHATE和LMNN+PCA两种方法。PHATE嵌入保留数据的全局流形结构，适合可视化复杂的非线性关系。LMNN+PCA嵌入通过度量学习强调时间点之间的分离，适合展示时间序列的演化模式。该组件提供拟合、转换和组合计算的接口，支持对新数据应用已学习的嵌入映射。

MetricsPlotter负责评估指标的可视化，生成横向对比的条形图展示不同模型在多个指标上的表现。该组件支持自动识别最佳模型并高亮显示，提供清晰的视觉对比。指标数据同时保存为CSV文件，支持进一步的统计分析。图表采用专业的配色方案和布局，确保可读性和美观性。

GenerationPlotter实现生成数据与真实数据的对比可视化，通过动态子图网格展示多个模型的生成结果。该组件支持不同嵌入类型的可视化，自动调整子图布局以适应模型数量。每个子图展示真实数据和生成数据在低维空间的分布，使用颜色编码时间点信息。图表同时保存为PNG和PDF格式，满足不同使用场景的需求。

## 执行流程与调用链

系统的执行流程分为三个主要阶段：训练阶段、评估阶段和可视化阶段。每个阶段通过独立的入口脚本启动，接收配置文件作为输入，自动完成相应的处理流程。

训练阶段由step1_run_experiment脚本驱动，该脚本接收实验配置文件路径作为命令行参数。执行流程首先通过ConfigLoader加载并合并所有相关配置文件，提取数据设置、模型配置和训练参数。然后根据数据配置创建相应的DataLoader实例，加载并预处理数据，划分训练集和测试集。对于配置中启用的每个模型，系统创建对应的模型实例和训练器实例，执行完整的训练循环直到收敛或达到早停条件。训练完成后，系统加载最佳检查点，在测试集上评估模型性能，计算所有标准指标并保存结果。

评估阶段可以独立于训练执行，通过step2_run_evaluation_only脚本启动。该脚本加载已训练好的模型检查点，在指定的测试集上重新计算评估指标。这种分离设计允许在不重新训练的情况下使用不同的评估策略或指标，提高了实验的灵活性。评估流程包括数据加载、模型加载、轨迹生成和指标计算四个步骤，每个步骤都有详细的日志输出。

可视化阶段由step3_multi_setting_visualization脚本驱动，该脚本支持同时加载多个实验设置的结果进行横向对比。执行流程通过MultiSettingVisualizationPipeline类协调，该类初始化所有Analyser模块的组件，包括DataManager、ModelManager、EmbeddingComputer、MetricsPlotter和GenerationPlotter。对于每个要对比的设置，系统加载相应的数据和模型，生成预测轨迹，计算降维嵌入，最后生成对比图表。可视化流程支持四种预定义的对比场景：EMT过程建模对比、时间点消融对比、时间点打乱对比和线性插值对比。

配置系统在整个执行流程中起到核心作用，所有的参数和设置都通过YAML文件定义。这种配置驱动的设计使得修改实验参数不需要改动代码，降低了出错的风险。配置文件的分层结构支持参数的复用和覆盖，避免了重复定义。系统在加载配置时进行完整性验证，确保所有必需的参数都已定义且格式正确。

## 数据流与状态管理

系统的数据流从原始的h5ad文件开始，经过一系列转换最终生成可视化结果。数据在不同模块之间传递时采用标准的格式和接口，确保兼容性和可追溯性。

原始数据以AnnData对象的形式存储在h5ad文件中，包含基因表达矩阵、细胞元数据和基因元数据。DataLoader模块读取h5ad文件后，首先进行高变基因筛选，将表达矩阵的列数降低到指定的基因数量。筛选后的数据按照时间标签过滤，只保留配置中指定的时间点。生物学划分验证确保训练集和测试集的分离，然后进行采样以满足配置中指定的样本数量要求。

处理后的数据被转换为NumPy数组，表达矩阵作为特征，时间标签作为目标。这些数组被封装为PyTorch的TensorDataset，进一步包装为DataLoader以支持批次迭代。训练过程中，DataLoader自动处理数据的批次化、打乱和设备转移，模型接收的是已经准备好的张量批次。

模型的输出根据类型不同有不同的形式。Schrödinger Bridge模型输出速度场，表示状态在当前时刻的瞬时变化率。Optimal Transport和VAE模型直接输出目标状态的预测。Batch OT模型输出一系列中间状态，通过顺序应用多个映射函数生成。所有模型的输出最终都转换为轨迹形式，即时间序列的状态序列，便于统一的评估和可视化。

评估指标的计算基于模型生成的轨迹和真实的目标状态。Evaluator模块接收这些数据，计算十个标准指标，包括损失、距离、相关性和分布匹配等多个维度。指标结果保存为JSON文件，包含每个模型在每个指标上的数值，以及一些元数据如评估时间和配置信息。

可视化流程从保存的结果文件中加载数据和指标，不需要重新运行模型推理。DataManager加载生成数据的pickle文件和指标的JSON文件，提取需要可视化的部分。EmbeddingComputer对真实数据和生成数据计算降维嵌入，将高维的基因表达空间投影到二维或三维空间。MetricsPlotter和GenerationPlotter分别生成指标对比图和嵌入可视化图，保存为图像文件供分析使用。

状态管理采用无状态设计，每个模块的方法调用不依赖于内部状态的累积，所有必需的信息都通过参数传递。这种设计提高了模块的可测试性和可复用性，避免了状态不一致导致的错误。模型的训练状态通过检查点文件持久化，包含模型参数、优化器状态和训练历史。检查点的保存和加载由Trainer模块统一管理，确保状态的完整性和一致性。

## 扩展性设计原则

系统的架构设计充分考虑了未来的扩展需求，通过抽象和接口隔离实现了高度的可扩展性。添加新功能通常只需要实现新的子类或组件，不需要修改现有代码，符合开闭原则。

添加新模型只需要继承BaseTrajectoryModel或其子类，实现必需的抽象方法。新模型自动获得统一的训练、评估和可视化支持，不需要修改训练器或评估器的代码。如果新模型需要特殊的训练逻辑，可以创建新的Trainer子类，重写批次处理和损失计算方法。模型的配置通过YAML文件定义，添加新模型只需要在模型配置文件中添加相应的条目。

添加新的数据处理策略只需要继承BaseDataLoader，重写数据处理的特定步骤。新的DataLoader自动集成到配置系统中，可以通过配置文件选择使用。如果需要新的采样策略或预处理方法，可以在子类中实现，不影响其他DataLoader的行为。数据配置的灵活性允许定义任意的时间点组合和采样参数，支持多样化的实验设计。

添加新的评估指标只需要在Evaluator类中添加新的计算方法，该方法接收预测和真实数据，返回指标值。新指标自动包含在评估报告中，可以在可视化中展示。指标的计算逻辑与模型训练完全解耦，可以独立开发和测试。如果需要新的可视化方式，可以创建新的Plotter组件，继承BaseVisualizer获得通用功能，实现特定的绘图逻辑。

配置系统的分层设计支持灵活的参数组合和覆盖。基础配置定义默认参数，实验配置可以选择性地覆盖这些参数。这种设计避免了配置的重复，同时保持了实验的可定制性。配置文件使用YAML格式，易于人工编辑和版本控制。配置的加载和验证逻辑集中在ConfigLoader中，确保配置的一致性和完整性。

代码组织遵循模块化原则，每个模块放在独立的目录中，包含相关的类和函数。模块之间通过明确的接口交互，避免循环依赖。每个模块的__init__文件定义了公开的API，隐藏内部实现细节。这种组织方式使得代码易于导航和理解，降低了维护成本。

