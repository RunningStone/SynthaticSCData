# 细胞状态转换学习补充实验设计方案

## 文档概述

本文档描述针对EMT数据集的六个补充实验，旨在从信息论和动力学保真度角度验证核心假设：细胞状态转换的熵增-熵减动力学使得边界条件不足以约束生成模型，中间状态特别是熵峰值时刻包含不可从边界推断的独立信息。

---

## 实验4：时间点系统消融实验

### 实验目的

通过系统性地移除单个中间时间点，量化每个时间点对模型性能的边际贡献，识别对准确建模最关键的时间窗口。该实验回答：是否所有中间时间点同等重要，还是存在信息密度更高的关键时刻？

### 数据设计

EMT数据集包含5个时间点的完整序列：正向过程有$t_0=0d, t_1=8h, t_2=1d, t_3=3d, t_4=7d$。我们设计3个消融变体，每个变体移除一个中间时间点（保留首尾$t_0$和$t_7$）。

对于每个消融变体$\mathcal{S}_{-i}$（表示移除时间点$t_i$），训练数据由剩余时间点构成。为保持总样本量恒定为$M$个细胞，我们在剩余时间点间重新分配样本。具体而言，若完整设置有$K$个时间点，每点$N$个细胞，则消融设置有$K-2$个时间点（刨除起点和终点），每点约$N' = \lfloor M/(K-2) \rfloor$个细胞。因此，这个实验需要首先用 /SynthaticSCData/step0_calculate_data_split_params.py  并设置bottleneck采样比例80% 来进行一次SB_MLP实验，然后依次移除时间点后重新设计采样数量。

测试始终在完整的测试集上进行，评估从初状态到终状态（先设计为）的预测精度，使用全部十个标准评估指标。

### 关键参数

每个消融变体$\mathcal{S}_{-i}$的参数配置如下。时间点集合为$\mathcal{T}_{-i} = \{t_0, t_1, \ldots, t_{i-1}, t_{i+1}, \ldots, t_5\}$，即移除$t_i$。每个时间点的采样数量为$N'_j = \lceil M / |\mathcal{T}_{-i}| \rceil$，确保总样本量约束。模型架构和训练超参数与原始Setting 2保持完全一致，包括SB-MLPlus的网络结构、学习率、批次大小和早停策略。注意这个实验只使用SB_MLP。

### 预期结果与分析

我们构建性能下降曲线$\Delta P(t_i) = P_{\text{full}} - P_{-i}$，分别为Evaluation metrics绘制这个曲线，$P_{\text{full}}$为使用全部5个时间点的基线性能，$P_{-i}$为移除$t_i$后的性能。

第一种可能的结果模式是均匀下降模式，即$\Delta P(t_i) \approx \text{const}$对所有中间时间点成立。这表明性能下降主要由数据量减少导致，每个时间点的贡献大致相等，不存在特别关键的时间窗口。这种模式削弱了"特定时间点携带关键动力学信息"的论点。

第二种可能模式是熵峰值敏感模式。如果$\Delta P(t_{\text{max entropy}}) \gg \Delta P(t_j)$对于$j \neq \text{max entropy}$显著成立，说明熵最大时刻的数据对模型学习探索-收敛动力学至关重要。这直接支持核心假设：系统在最不确定时刻的状态分布包含无法从其他时刻外推的信息。

第三种可能模式是阶段依赖模式。如果早期时间点（$t_1, t_2$）的移除影响大于晚期时间点（$t_5, t_6$），或反之，说明动力学的不同阶段对建模难度有非对称贡献。例如，若移除早期响应时间点导致更大性能损失，可能表明细胞命运决定的关键事件发生在转换初期。

定量分析包括计算边际信息贡献$I_{\text{margin}}(t_i) = \Delta P(t_i) / P_{\text{full}}$，表示该时间点贡献的相对性能提升比例。识别关键时间点集合$\mathcal{T}_{\text{critical}} = \{t_i : I_{\text{margin}}(t_i) > \theta\}$，其中阈值$\theta$可设为平均边际贡献的1.5倍。这些关键时间点在资源受限的实验设计中应被优先采样。

### 生物学解释

若熵最大点$t_4=7d$被识别为关键时间点，这与EMT的生物学机制一致：该时刻细胞处于上皮和间充质状态之间的混合态，基因调控网络处于双稳态之间的过渡区域，此时的状态分布最宽，携带最多关于可达状态空间边界的信息。

若早期时间点$t_1=8h$显示高重要性，可能反映TGF-β信号通路激活后的快速转录响应，这些早期分子事件决定后续轨迹的方向。若逆转过程的时间点$t_5, t_6$重要性低，可能说明逆转轨迹较为确定性，缺少探索阶段，可以从峰值状态和终点较准确地插值。

---

## 实验5：时间信息解耦实验（时间打乱）

### 实验目的

区分模型性能提升的两个可能来源：时间的因果顺序信息与状态空间的几何覆盖。通过破坏时间顺序但保持状态空间采样，测试模型是否真正学习了时间依赖的动力学，还是仅仅记忆了高维空间中的插值映射。

### 数据设计

基于Setting 2的完整数据集（5个时间点，每点K个细胞），我们构造1个对照组。

正常时序组$\mathcal{D}_{\text{ordered}}$保持原始时间配对结构（也就是目前的setting2训练好的）。训练样本为连续时间点间的细胞配对$(x_i^{(t_j)}, x_i^{(t_{j+1})})$，其中$x_i^{(t_j)}$表示时间点$t_j$的第$i$个细胞，时间编码为真实的物理时间$t_j$和$t_{j+1}$。

打乱时序组$\mathcal{D}_{\text{shuffled}}$破坏时间的因果结构（为了本实验新增的）。对于每个训练样本，我们从所有时间点的细胞池中随机抽取两个细胞$x_p^{(t_m)}$和$x_q^{(t_n)}$，形成配对$(x_p^{(t_m)}, x_q^{(t_n)})$。为避免引入生物学上不合理的长时间跨度，我们约束$|t_m - t_n|$的分布与真实数据中相邻时间点的时间间隔分布一致。

具体采样策略如下。首先统计真实数据中相邻时间点间的时间间隔分布$P(\Delta t)$，例如$\Delta t \in \{8h, 16h, 48h, 96h\}$及其频率。在构造打乱数据时，先从$P(\Delta t)$中采样一个时间间隔$\delta$，然后随机选择起点时间$t_m$，设定终点时间$t_n = t_m + \delta$（若$t_n$超出范围则舍弃）。最后从$t_m$和$t_n$的细胞池中各随机抽取一个细胞配对。这确保时间间隔的统计分布保持不变，但因果关系被破坏。

关键的是，打乱组的模型仍然接收时间编码输入。对于配对$(x_p^{(t_m)}, x_q^{(t_n)})$，模型的输入为起点细胞$x_p$和时间标签$t_m$，目标是预测终点细胞$x_q$在时间$t_n$的状态。时间编码$t_m$和$t_n$的数值与正常组相同，但它们与细胞的真实时间身份解耦。

### 关键参数

两组使用相同的样本总量M，相同的SB-MLPlus模型架构和训练超参数(只需补充乱序的SB_MLP训练即可)。测试在相同的测试集上进行，评估从初态到终态的标准预测任务。对比指标包括十个标准评估指标以及额外的轨迹连续性指标。

轨迹连续性指标定义如下。生成从$t_0$到$t_7$的完整轨迹$\{x^{(t_0)}, x^{(t_1)}, \ldots, x^{(t_7)}\}$，计算相邻时间点间的平均跳跃距离$J = \frac{1}{7}\sum_{j=0}^{6} \mathbb{E}[\|x^{(t_{j+1})} - x^{(t_j)}\|_2]$。真实数据的轨迹应该在高维空间中相对平滑，跳跃距离较小。如果模型没有学到时间的连续性，生成的轨迹可能出现不自然的跳跃。

### 预期结果与分析

第一种情况：打乱组性能显著下降。如果$P_{\text{shuffled}} \ll P_{\text{ordered}}$，例如主要指标下降超过30%，说明模型的成功高度依赖时间的因果顺序信息。这证明模型不是简单的状态空间插值器，而是真正学习了时间依赖的动力学规律。特别地，若Fréchet距离和轨迹连续性指标在打乱组中大幅恶化，说明模型需要时间信息来捕捉状态转换的平滑性和方向性。

第二种情况：打乱组性能保持。如果$P_{\text{shuffled}} \approx P_{\text{ordered}}$，性能差异在10%以内，说明模型主要利用了状态空间的几何结构而非时间的因果关系。这暴露了当前方法可能仅在做高维插值，类似于学习一个从"状态空间某区域"到"另一区域"的映射，而时间标签$t$仅起到"选择映射强度"的作用。这种情况下，完整轨迹的优势可能仅来自于更密集的状态空间覆盖，而非动力学建模的本质需求。

第三种情况：部分指标敏感部分不敏感。如果点估计精度（MAE, PCC）在打乱组中保持，但分布质量指标（FD, Wasserstein, 轨迹连续性）显著下降，说明模型在空间平均意义上能找到正确的目标区域，但无法生成物理上合理的轨迹。这仍然支持时间信息的必要性，但表明其作用更多体现在轨迹的全局结构而非局部预测。

### 理论意义

该实验直接检验Schrödinger Bridge框架的核心价值主张。SB的理论优势在于显式建模时间依赖的漂移场$b(x,t)$，学习连续时间动力学而非离散映射。如果打乱时间后性能崩溃，验证了这一理论优势。如果性能不变，说明SB在实际应用中可能退化为条件生成模型，其时间建模能力未被充分利用或验证。

从信息论角度，正常时序组中的样本$(x^{(t_j)}, x^{(t_{j+1})})$携带条件互信息$I(X_{t_{j+1}}; X_{t_j} | T=t_j)$，其中时间条件$T$提供了因果方向。打乱组破坏了这种条件依赖，将问题转化为边缘分布上的学习。性能差异量化了因果时间信息的贡献。

---

## 实验6：合成中间状态对照实验

### 实验目的

测试边界数据通过几何插值能否达到真实中间状态的效果，从而区分"状态空间采样密度"与"真实动力学信息"的贡献。如果简单插值就能接近Setting 2的性能，说明问题主要在于数据覆盖不足；如果插值失败，说明真实中间状态包含非线性、不可插值的动力学特征。

### 数据设计

基于Setting 1的边界数据$\mathcal{D}_{\text{boundary}} = \{(x_i^{(t_0)}, x_i^{(t_7)})\}_{i=1}^{3000}$，我们通过三种插值方法生成伪中间状态，构造增强训练集。

线性插值方法最为简单。对于每对边界细胞$(x_i^{(t_0)}, x_i^{(t_7)})$，在时间点$t_k \in \{t_1, t_2, \ldots, t_6\}$处生成插值状态$\tilde{x}_i^{(t_k)} = \lambda_k x_i^{(t_0)} + (1-\lambda_k) x_i^{(t_7)}$，其中权重$\lambda_k = \frac{t_7 - t_k}{t_7 - t_0}$根据时间比例线性分配。这生成了6个伪中间时间点，每点3000个细胞。为匹配Setting 2的总样本量6000，我们从每个时间点采样约750个细胞。

生成的增强数据集分别记为$\mathcal{D}_{\text{linear}}$。数据集包含5个时间点，每点约750个细胞（边界点使用真实数据，中间点使用插值数据），总量匹配Setting 2。

### 关键参数

三个对照组与Setting 2使用相同的模型架构去训练setting2中的所有模型。测试在真实测试集上进行，评估所有标准指标。额外计算中间时间点预测的专项误差$E_{\text{mid}}(t_k) = \text{MAE}(x_{\text{pred}}^{(t_k)}, x_{\text{real}}^{(t_k)})$，分别对每个中间时间点$t_k \in \{t_1, \ldots, t_6\}$评估。

### 预期结果与分析

第一种结果：插值有效。如果$P_{\text{linear}} \approx P_{\text{Setting 2}}$，性能差距在15%以内，说明真实中间状态的主要价值在于增加状态空间覆盖。边界数据已包含足够信息通过几何推理重构中间状态，问题本质是数据稀疏而非动力学复杂。这削弱了"需要真实中间状态捕捉探索动力学"的论点，暗示问题可能通过数据增强或更好的插值算法解决。

第二种结果：插值部分有效。如果$P_{\text{Setting 1}} < P_{\text{linear}} < P_{\text{Setting 2}}$，形成性能梯度，说明插值方法的复杂度影响效果。简单线性插值失败但GP插值接近真实数据，暗示中间状态的非线性特征可以通过统计学习近似。但真实数据仍有优势，可能因为插值无法完全捕捉细胞异质性或罕见子群。

第三种结果：插值失败。如果$P_{\text{linear}} \ll P_{\text{Setting 2}}$，例如仍接近$P_{\text{Setting 1}}$水平，说明真实中间状态包含根本无法从边界推断的信息。这强烈支持核心假设：细胞在熵增阶段探索的状态空间区域与边界状态所在的流形不同，插值沿着边界间的测地线运动，但真实轨迹偏离这条路径进入高维空间的其他区域。

定量分析包括计算插值残差的结构化程度。对于每个中间时间点$t_k$，计算真实数据与最佳插值方法的差异$R_k = X_{\text{real}}^{(t_k)} - X_{\text{interpolated}}^{(t_k)}$。对残差进行主成分分析，若前10个主成分解释残差方差的比例超过70%，说明残差包含系统性的、低秩的结构化信息。进一步检验残差是否与生物学标签（如细胞类型、marker基因表达）相关，若显著相关，说明插值失败源于生物学机制而非纯粹的统计噪声。

### 理论洞察

该实验连接几何观点与动力学观点。OT理论假设状态转换沿着Wasserstein空间的测地线进行，这对应于最优质量输运。如果OT插值成功，说明细胞转换确实遵循这一几何原理。如果失败，说明生物学动力学不遵守最优传输假设——细胞不是"最经济地"从起点移动到终点，而是经历了探索性的、熵增的绕行。

从动力学系统角度，真实轨迹由微分方程$\frac{dx}{dt} = f(x,t)$支配。插值隐含假设$f$可以从边界条件唯一确定，这在线性系统或强约束系统中成立。失败说明EMT是弱约束系统，存在多条连接边界的可能路径，真实路径由初始条件以外的因素（如随机涨落、外部信号时序）决定。中间时间点的数据打破简并，提供额外约束确定唯一解。

---

## 实验7：生成轨迹的熵演化保真度测试

### 实验目的

直接评估模型生成的时间轨迹是否重现真实数据的核心动力学特征：熵的非单调演化（熵增-熵减过程）。该实验是对核心科学假设的最直接验证，检验不同setting的模型能否捕捉细胞状态转换中的"探索-收敛"两阶段动力学。

### 熵估计方法

对于高维单细胞数据$X \in \mathbb{R}^{d}$（$d=1000$基因），直接估计微分熵$H(X) = -\int p(x)\log p(x)dx$面临维度灾难。我们采用两种互补的非参数方法。

K近邻熵估计基于Kozachenko-Leonenko estimator。给定$N$个样本$\{x_1, \ldots, x_N\}$，熵估计为$\hat{H}_{\text{KNN}} = \frac{d}{N}\sum_{i=1}^{N}\log(\rho_k(x_i)) + \log(N-1) - \psi(k) + \log(c_d)$，其中$\rho_k(x_i)$是第$i$个样本到其第$k$近邻的欧氏距离，$\psi$是Digamma函数，$c_d = \frac{\pi^{d/2}}{\Gamma(d/2+1)}$是$d$维单位球体积。我们选择$k=5$作为近邻数，在偏差-方差间取得平衡。该方法的优势是无需假设分布形式，适用于任意形状的高维分布。

多元高斯近似方法假设数据近似服从多元正态分布。先计算样本协方差矩阵$\Sigma = \frac{1}{N-1}\sum_{i=1}^{N}(x_i-\bar{x})(x_i-\bar{x})^T$，应用Ledoit-Wolf收缩估计器进行正则化以改善高维下的病态性，得到$\tilde{\Sigma} = (1-\alpha)\Sigma + \alpha\frac{\text{tr}(\Sigma)}{d}I$，其中$\alpha$由数据自动确定。熵估计为$\hat{H}_{\text{Gauss}} = \frac{1}{2}\log\det(2\pi e \tilde{\Sigma}) = \frac{d}{2}\log(2\pi e) + \frac{1}{2}\log\det(\tilde{\Sigma})$。行列式计算使用数值稳定的$\text{slogdet}$函数避免上溢。该方法计算高效且在数据接近正态时精确，但在多模态或重尾分布时有偏。

两种方法的结合提供鲁棒性。若两者趋势一致，增强结论可信度；若不一致，说明分布的非高斯性对熵估计影响显著，需进一步分析。

### 数据设计

对于Setting 1, Setting 3和Setting 2三个实验组，我们用训练好的SB_MLP生成完整的时间轨迹来评估熵演化。

真实轨迹作为金标准。从测试集中，对每个时间点$t_j \in \{t_0, t_1, \ldots, t_7\}$采样$M=1000$个细胞，构成真实轨迹的横截面$\mathcal{X}_{\text{real}}^{(t_j)} = \{x_1^{(t_j)}, \ldots, x_M^{(t_j)}\}$。对每个时间点计算熵估计$H_{\text{real}}(t_j)$，绘制真实熵曲线$H_{\text{real}}(t)$。

模型生成轨迹按以下方式构造。从测试集的$t_0$采样$M=1000$个初始细胞$\{x_1^{(t_0)}, \ldots, x_M^{(t_0)}\}$。对于每个初始细胞$x_i^{(t_0)}$和目标时间点$t_j$，使用训练好的SB_MLP生成预测$\tilde{x}_i^{(t_j)} = \text{SB_MLP}(x_i^{(t_0)}, t_j)$。收集所有生成的$\{\tilde{x}_1^{(t_j)}, \ldots, \tilde{x}_M^{(t_j)}\}$，构成生成轨迹在$t_j$的横截面，计算熵$H_{\text{gen}}(t_j)$。

对Setting 1, Setting 3, Setting 2分别重复上述过程，得到三条生成熵曲线$H_{\text{S1}}(t), H_{\text{S3}}(t), H_{\text{S2}}(t)$。

### 关键参数与指标

熵估计中，KNN方法的近邻数$k=5$，每时间点采样细胞数$M=1000$。SDE求解的时间步长$\Delta t=0.01$，扩散系数$\sigma=\sqrt{2D}$其中$D=0.1$与训练时一致。

定量评估指标包括以下四个方面。

熵曲线相似度使用动态时间规整距离$\text{DTW}(H_{\text{real}}, H_{\text{gen}})$或直接的均方误差$\text{MSE} = \frac{1}{8}\sum_{j=0}^{7}(H_{\text{real}}(t_j) - H_{\text{gen}}(t_j))^2$。DTW对时间轴的轻微错位鲁棒，更符合生物学轨迹的不确定性。

熵峰值检测判断生成轨迹是否展现非单调性。定义峰值时刻为$t^* = \arg\max_t H(t)$，峰值幅度为$A = H(t^*) - \min\{H(t_0), H(t_7)\}$。真实数据应有显著峰值，例如$t^* \approx t_4=7d$且$A > 0$。检验每个模型生成轨迹是否满足：存在$t_k$使得$H(t_k) > H(t_0)$且$H(t_k) > H(t_7)$，即熵曲线呈倒U型而非单调。

熵峰值位置误差衡量模型是否识别正确的"最不确定时刻"。计算$\Delta t^* = |t^*_{\text{gen}} - t^*_{\text{real}}|$。如果模型错误地将熵峰值放在早期或晚期，说明它误解了状态转换的时间尺度。

熵变速率分析比较不同阶段的熵演化速度。将时间分为探索阶段$[t_0, t^*]$和收敛阶段$[t^*, t_7]$，计算各阶段的平均熵变速率$\dot{H}_{\text{explore}} = \frac{H(t^*)-H(t_0)}{t^*-t_0}$和$\dot{H}_{\text{collapse}} = \frac{H(t_7)-H(t^*)}{t_7-t^*}$。真实数据中，$\dot{H}_{\text{explore}} > 0$且$\dot{H}_{\text{collapse}} < 0$，幅度可能不对称。模型应重现这种非对称性。

### 预期结果与分析

第一种预期结果：Setting 1失败捕捉非单调性。$H_{\text{S1}}(t)$呈现从$H(t_0)$到$H(t_7)$的单调变化（单调增或单调减），无明显峰值。这说明仅用边界训练的模型学习到一个简单的线性或单调插值，无法捕捉中间的探索阶段。定量上，$A_{\text{S1}} \approx 0$或$A_{\text{S1}} \ll A_{\text{real}}$，峰值检测失败。这直接验证了核心假设的一半：边界条件不足以约束熵演化的非单调性。

第二种预期结果：Setting 3部分重现非单调性。$H_{\text{S3}}(t)$显示峰值，$t^*_{\text{S3}} \approx t^*_{\text{real}}$，但峰值幅度$A_{\text{S3}} < A_{\text{real}}$或熵曲线的精细结构与真实曲线有偏差。例如，生成的熵峰值可能过于尖锐或平滑。这说明给定熵最大点的信息，模型能学到探索-收敛的框架，但缺少其他中间点导致对动力学细节的拟合不足。熵变速率的非对称性可能被捕捉但不精确。

第三种预期结果：Setting 2准确重现熵演化。$H_{\text{S2}}(t)$在形状、峰值位置、幅度和熵变速率上都接近$H_{\text{real}}(t)$，$\text{DTW}(H_{\text{real}}, H_{\text{S2}}) < \text{DTW}(H_{\text{real}}, H_{\text{S1/S3}})$且$\Delta t^*_{\text{S2}} < \Delta t^*_{\text{S3}}$。这证明完整轨迹训练使模型学习到真实的动力学过程，不仅预测终点准确，而且路径物理上合理。

交叉验证通过比较KNN和高斯方法的结果。若两种方法给出一致的结论（例如都显示Setting 1无峰值，Setting 2有峰值），说明结论对估计方法鲁棒。若不一致，需检查数据的多模态性或异质性是否导致高斯假设失效。

### 生物学解释

若Setting 1的生成轨迹显示单调熵减$H(t_0) > H(t_j) > H(t_7)$，可能反映模型错误地假设细胞直接从异质的上皮状态收敛到更均一的间充质状态。这忽略了EMT过程中细胞实际经历的分子去稳定化阶段：在TGF-β持续刺激下，上皮基因被抑制但间充质基因尚未充分激活，细胞处于"双低"状态，群体异质性增加，熵升高。

若Setting 3成功重现熵峰值，说明峰值时刻的数据作为"锚点"指导模型理解状态空间的可达边界。生物学上，7天时的细胞分布最分散，这些数据告诉模型"系统能探索到多远"，从而约束了中间轨迹。

若Setting 2的熵曲线在1-3天间显示快速上升随后缓慢下降，对应已知的EMT时间尺度：早期转录响应快速但不协调（熵增快），晚期表观遗传重塑缓慢但确定性强（熵减慢）。模型捕捉这种速率非对称性表明它学到了多时间尺度的动力学。

---

## 实验8：中间状态的信息论不可推断性证明

### 实验目的

从信息论角度严格证明熵最大时刻的状态分布包含无法从边界状态推断的独立信息。该实验量化中间状态的"信息增益"，回答核心问题：边界数据$(X_{t_0}, X_{t_7})$是否提供足够信息重构$X_{t_{max}}$，其中$t_{max}$是熵最大时刻。

### 理论框架

信息论中，随机变量$Y$在给定$X$条件下的不确定性由条件熵$H(Y|X)$度量。如果$H(X_{t_{max}}|X_{t_0}, X_{t_n})$接近$H(X_{t_{max}})$，说明边界变量几乎不提供关于中间状态的信息，$X_{t_{max}}$基本独立。如果$H(X_{t_{max}}|X_{t_0}, X_{t_n}) \ll H(X_{t_{max}})$，说明中间状态可以从边界较准确推断。

等价地，我们可以计算互信息$I(X_{t_{max}}; X_{t_0}, X_{t_n}) = H(X_{t_{max}}) - H(X_{t_{max}}|X_{t_0}, X_{t_n})$。若互信息低，说明边界对中间状态的解释力有限。

因为我们已经在setting1和setting2中分别训练了SB模型，这两个模型共享起点和终点（0-1）。于是，检查数据设定即可知道 8h 在setting2中被映射在中间某个值。让这两个模型都给定0d，生成8h的样本获得 8h_s1 和 8h_s2。

用Kraskov-Stögbauer-Grassberger (KSG) 估计器这个互信息估计器就可以直接在原始数据上估计互信息了【有0d-8h 和 7d- 8h两种情况】。

KGS估计I(X;Y) 需要从联合分布PX,YP_{X,Y} 采样的配对样本(xi,yi)，以及 需要从边缘分布乘积PX×PY 。
有三种情况
mi_0d = KSG(X_0d, X_8h_real)
mi_0d_s1 = KSG(X_0d, X_8h_s1) 
mi_0d_s2 = KSG(X_0d, X_8h_s2)
和
mi_7d = KSG(X_7d, X_8h_real)
mi_7d_s1 = KSG(X_7d, X_8h_s1) 
mi_7d_s2 = KSG(X_7d, X_8h_s2)
注意KGS需要先降维到50， 然后绘制heat matrix（带mi的数字）即可

另一方面，计算生成数据和真实数据之间的MMD。这个MMD应该和真实的差不多就对了。

mmd_0d_real = MMD(X_0d, X_8h_real)
mmd_0d_s1 = MMD(X_0d, X_8h_s1) 
mmd_0d_s2 = MMD(X_0d, X_8h_s2)
和
mmd_7d_real = MMD(X_7d, X_8h_real)
mmd_7d_s1 = MMD(X_7d, X_8h_s1) 
mmd_7d_s2 = MMD(X_7d, X_8h_s2)
---


## 实验总结与整合分析

### 实验间的逻辑链条

六个实验形成递进式论证，每个实验聚焦不同侧面。

实验3（时间点消融）建立边际贡献图谱，识别关键时间窗口，为后续实验提供优先级。若熵最大点被识别为最关键，强化实验3-6的设计逻辑。

实验4（时间打乱）区分时间因果信息与空间覆盖，为核心论点提供正交验证。若打乱后性能崩溃，证明时间顺序不可或缺；若保持，需重新审视方法。

实验5（合成插值）排除"简单几何就足够"的竞争解释。若插值失败，证明中间状态包含非线性、不可插值的信息，推动向实验5的信息论分析。

实验6（熵演化）提供现象层面的直接可视化证据，是对外沟通最有力的图。非专业读者能直观理解"熵曲线不匹配"意味着"模型没学对"。

实验7（信息论证明）提供理论层面的严格量化，将"感觉上中间状态重要"转化为"残差包含X比特的独立信息"。这是论文的理论高度所在。


### 多实验交叉验证的决策树

如果实验6（熵演化）和实验8（几何探索）都显示Setting 1缺乏非单调动态，且实验5（插值）显示所有插值方法失败，且实验7（信息论）显示残差高度结构化，则强一致性结论：中间状态包含根本不可从边界推断的动力学信息，核心假设成立。

如果实验5（插值）中GP或OT接近Setting 2，但实验6（打乱）显示性能崩溃，则混合解释：几何上中间状态可近似重构，但时间因果链对学习至关重要。问题不在"缺数据"而在"缺时序"。

如果实验7（信息论）显示MLP预测达到$R^2>0.9$，但实验5（消融）仍显示Setting 2显著优于Setting 1，则可能是优化或模型容量问题。边界信息理论上充分，但现有模型架构或训练方法未能有效提取。这引导未来工作改进模型而非数据收集。

### 对核心论点的综合支持

六个补充实验协同证明：细胞状态转换的熵增-熵减动力学不是边界条件的简单插值后果，而是需要中间时间点数据提供额外约束的复杂动力学系统。熵最大时刻的数据作为关键锚点，既提供状态空间探索边界的信息（几何约束），也提供动力学时间尺度的信息（时序约束）。缺少这些信息，模型退化为简单插值器，无法生成物理上合理的轨迹，在外推任务中失败，且在需要机制解释的应用（如药物筛选、细胞工程）中不可靠。

这些发现对实验设计有直接指导意义：资源有限时，应优先采样熵最大时刻和关键转换节点，而非均匀时间网格。

对建模方法有理论启示：生成模型需要显式建模时间依赖的动力学，纯粹的状态空间映射（如VAE, GAN）不足以捕捉生物学过程。

对领域有广泛影响：类似论证可扩展到其他涉及非单调动力学的系统，如免疫激活、细胞周期、疾病进展，推动单细胞生物学从"快照"范式转向"电影"范式。